name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up JDK 11
        uses: actions/setup-java@v1
        with:
          java-version: 11

      - name: Build with Maven
        run: mvn clean package

      - name: Build Docker image
        run: docker build -t saviosoaresufc/voucasar-server:latest .

      - name: Log in to Docker Hub
        run: echo ${{ secrets.TOKEN_ACCESS }} | docker login -u ${{ secrets.TOKEN_USERNAME_LOW }} --password-stdin

      - name: Push Docker image
        run: docker push saviosoaresufc/voucasar-server:latest

      - name: Deploy to server
        run: |
          ssh saviosoaresufc@SávioCarvalho 'docker pull saviosoaresufc/voucasar-server:latest && docker stop voucasar-server-1 && docker rm voucasar-server-1 && docker run -d --name voucasar-server-1 saviosoaresufc/voucasar-server:latest'
